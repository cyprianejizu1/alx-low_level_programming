#include "main.h"

/**
* main - this print alphabet in lowercase
*
* Return: alway return 0
*
*/

void print_alphabet(void)
{
	char a;
	for (a = 'a'; a <= 'z'; a++)
	putchar(a);
	putchar('\n');
	return (0);
}
